// Простая программа
переменная имя = "Мир";
летопись.запись("Привет, " + имя + "!");

// Условный оператор
если (имя === "Мир") {
    летопись.запись("Привет, Мир!");
} иначе {
    летопись.запись("Привет, незнакомец!");
}
