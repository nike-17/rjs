name: Run tests and upload coverage

on: 
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    name: Run tests and collect coverage
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          
      - name: Configure npm and install dependencies
        run: |
          # Clear npm cache and reset any proxy settings
          npm cache clean --force
          npm config delete proxy
          npm config delete https-proxy
          npm config delete registry
          npm config set registry https://registry.npmjs.org/
          
          # Install specific npm version
          npm install -g npm@10.5.0
          
          # Log versions for debugging
          node -v
          npm -v
          
          # Configure npm
          npm config set fund false
          npm config set audit false
          
          # Install dependencies
          npm ci --registry=https://registry.npmmirror.com --no-audit --no-fund --prefer-offline --no-package-lock
          
          # Install type definitions
          npm install --no-save @types/node@latest @types/jest@latest
        
      - name: Build
        run: npm run build

      - name: Run tests
        run: npm test -- --coverage

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./coverage/coverage-final.json
          fail_ci_if_error: false

  release:
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          
      - name: Clear npm cache and verify installation
        run: |
          npm cache clean --force
          npm install -g npm@10.5.0
          node -v
          npm -v
          
      - name: Install dependencies
        run: |
          npm config set fund false
          npm config set audit false
          npm ci --prefer-offline --no-audit --no-fund
      
      - name: Build
        run: npm run build
      
      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}
