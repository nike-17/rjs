---
title: Синтаксис
slug: /syntax
---

# Справочник по синтаксису Russian JS

В этом разделе представлен полный справочник по синтаксису Russian JS.

## Переменные и константы

```rjs
// Объявление переменных
переменная имя = "Мир";
константа ВОЗРАСТ = 25;

// Несколько переменных
переменная x = 10, y = 20, z = 30;

// Деструктуризация
переменная [первый, второй] = [1, 2];
переменная { имя, возраст } = пользователь;
```

## Управляющие конструкции

### Условные операторы

```rjs
// if-else
если (возраст >= 18) {
  консоль.лог("Вы совершеннолетний");
} иначе если (возраст >= 13) {
  консоль.лог("Вы подросток");
} иначе {
  консоль.лог("Вы ребенок");
}

// Тернарный оператор
константа статус = возраст >= 18 ? "взрослый" : "ребенок";
```

### Циклы

```rjs
// for
для (переменная i = 0; i < 10; i++) {
  консоль.лог(i);
}

// for...of
для (переменная элемент из массива) {
  консоль.лог(элемент);
}

// while
пока (условие) {
  // код
}

// do-while
делать {
  // код
} пока (условие);
```

## Функции

```rjs
// Функция
функция сложить(а, б) {
  вернуть а + б;
}

// Стрелочная функция
константа умножить = (а, б) => а * б;

// Асинхронная функция
асинхронная функция загрузитьДанные() {
  константа ответ = ждать fetch('https://api.example.com/data');
  константа данные = ждать ответ.вJson();
  вернуть данные;
}
```

## Классы

```rjs
класс Животное {
  конструктор(имя) {
    это.имя = имя;
  }

  говорить() {
    консоль.лог(`${это.имя} издает звук.`);
  }
}

класс Собака расширяет Животное {
  говорить() {
    консоль.лог(`${это.имя} лает: Гав-гав!`);
  }
}

константа пес = новый Собака('Рекс');
пес.говорить(); // Выведет: Рекс лает: Гав-гав!
```

## Обработка ошибок

```rjs
попробовать {
  // Код, который может вызвать ошибку
  если (ошибка) {
    выбросить новая Ошибка("Что-то пошло не так");
  }
} поймать (ошибка) {
  консоль.ошибка("Произошла ошибка:", ошибка.сообщение);
} наконец {
  // Код, который выполнится в любом случае
  консоль.лог("Завершение обработки");
}
```

## Встроенные объекты

```rjs
// Консоль
консоль.лог("Обычное сообщение");
консоль.ошибка("Сообщение об ошибке");
консоль.предупреждение("Предупреждение");
консоль.информация("Информационное сообщение");

// Массивы
константа числа = [1, 2, 3, 4, 5];
константа удвоенные = числа.отобразить(х => х * 2);
константа сумма = числа.редуцировать((а, б) => а + б, 0);

// Объекты
константа пользователь = {
  имя: "Иван",
  возраст: 30,
  приветствовать() {
    вернуть `Привет, меня зовут ${это.имя}`;
  }
};
```

## Модули

```rjs
// Импорт
импортировать { функция1, функция2 } из './модуль';
импортировать * как утилиты из './утилиты';

// Экспорт
экспорт константа КОНСТАНТА = 42;
экспорт функция привет() { вернуть "Привет!"; };
```

## Шаблонные строки

```rjs
константа имя = "Мир";
константа приветствие = `Привет, ${имя}!
Сегодня ${новая Дата().toLocaleDateString()}.`;

// Многострочные строки
константа html = `
  <div>
    <h1>${заголовок}</h1>
    <p>${текст}</p>
  </div>
`;
```

## Полная таблица соответствий

| Russian JS | JavaScript |
|------------|------------|
| `переменная` | `var` |
| `константа` | `const` |
| `если` | `if` |
| `иначе` | `else` |
| `для` | `for` |
| `пока` | `while` |
| `делать` | `do` |
| `функция` | `function` |
| `вернуть` | `return` |
| `класс` | `class` |
| `новый` | `new` |
| `это` | `this` |
| `супер` | `super` |
| `импортировать` | `import` |
| `экспорт` | `export` |
| `по_умолчанию` | `default` |
| `асинхронная` | `async` |
| `ждать` | `await` |
| `попробовать` | `try` |
| `поймать` | `catch` |
| `наконец` | `finally` |
| `выбросить` | `throw` |
| `истина` | `true` |
| `ложь` | `false` |
| `неопределено` | `undefined` |
| `нуль` | `null` |
| `из` | `from` |
| `как` | `as` |
| `в` | `in` |
| `экземпляр` | `instanceof` |
| `тип` | `typeof` |
| `удалить` | `delete` |
| `создать` | `new` |
| `выполнить` | `do` |
| `прервать` | `break` |
| `продолжить` | `continue` |
| `переключатель` | `switch` |
| `случай` | `case` |
| `консоль` | `console` |
| `лог` | `log` |
| `ошибка` | `error` |
| `предупреждение` | `warn` |
| `информация` | `info` |
