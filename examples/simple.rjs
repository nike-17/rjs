// Простая программа
переменная имя = "Мир";
консоль.лог("Привет, " + имя + "!");

// Условный оператор
если (имя === "Мир") {
    консоль.лог("Привет, Мир!");
} иначе {
    консоль.лог("Привет, незнакомец!");
}
